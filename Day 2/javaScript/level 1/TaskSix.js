const prompt = require("prompt-sync")();
const length = Number(prompt("enter length: "));
const width = Number(prompt("enter width: ");

let area = length * width;
console.log("area of the two numbers is: " + area);